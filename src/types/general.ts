export interface appState {
    collapsed: boolean;
    hide: boolean;
    searchHide: boolean;
    activeSearch: boolean;
}

export interface layoutMode {
    darkMode: boolean;
    rtl: boolean;
    topMenu: boolean;
}